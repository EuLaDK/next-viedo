# Next Video 后续开发清单

更新日期：2026-07-03

## 当前已完成

- [x] shadcn/ui 基础组件接入
- [x] Zustand 本地状态管理接入
- [x] 首页：首页骨架、顶部导航、频道导航、主推区、推荐区、热播区、排行榜入口
- [x] 频道页：频道 hero、筛选、排序、视频网格、频道热榜
- [x] 搜索页：关键词搜索、热门词、搜索历史、类型筛选、排序
- [x] 播放页：静态视频播放、选集、详情、相关推荐、弹幕、评论、点赞、分享、收藏、缓存、评论排序、弹幕颜色
- [x] 观看历史：自动记录播放进度，支持从历史回到上次播放秒数
- [x] 个人中心：概览、观看历史、收藏、缓存中心、VIP 页面
- [x] 排行榜页：热度榜、高分榜、新片榜、飙升榜、口碑榜、VIP 榜、频道筛选
- [x] Header 增强：移动端搜索、用户下拉菜单、登录/VIP 本地演示态
- [x] 共享视频卡片组件抽取
- [x] 用户资料、追剧收藏、观看历史接入 Go `/me` API facade，并保留本地 fallback
- [x] 本地联调 Go `/me` API：账号同步测试、lint、核心页面请求和账号写入读取链路通过
- [x] 评论和弹幕接入 Go `/videos/{id}/comments`、`/videos/{id}/danmaku` API facade，并保留本地 fallback
- [x] 播放页接入 Go 返回的 `playback` 配置：播放源、默认清晰度、试看秒数和会员内容标记已进入 API 响应
- [x] `PlayerShell` 使用 `playback.sources` 播放视频，并增加清晰度选择控件
- [x] Go 后端新增 `video_playback_sources` 表，播放详情可返回同一视频多清晰度播放源
- [x] Go 后端播放详情已按账号 VIP 状态计算 `canPlay` 和 `trialSeconds`
- [x] VIP 页面套餐购买状态同步到 Go `/me/vip`，并保留本地 fallback
- [x] 播放详情返回 `playback.resume`，播放页无 URL 秒数时使用服务端恢复点
- [x] 前端 HLS/DASH 播放引擎适配：按 `mimeType`/URL 识别 MP4、HLS、DASH，HLS 使用 `hls.js`，DASH 使用 `dashjs`，并保留原生 HLS 和 MP4 直连。
- [x] 播放器控制修复：清晰度、倍速、弹幕速度改用 shadcn/Radix 菜单，修复同一播放地址切清晰度和下一集时 video 源未重新绑定的问题。
- [x] 真实登录注册 v1：Go 后端支持邮箱密码注册/登录和密码哈希，前端登录弹窗改为登录/注册二合一，并在登录态自动带 `X-User-ID` 请求头。
- [x] 登录/注册错误提示 v1：真实接口返回登录失败、重复邮箱、弱密码等错误时，前端弹窗展示中文提示并保持打开。
- [x] session cookie v1：前端 API 请求已带 `credentials: "include"`，Go 登录/注册成功后可通过 httpOnly Cookie 维持登录态；开发期 `X-User-ID` 仍保留兼容。
- [x] 前端 Cookie 会话恢复 v1：页面启动同步 `/me` 时不再带开发期 `X-User-ID`，优先通过 `next_video_session` Cookie 判断真实登录态。
- [x] 用户资料编辑 v1：个人中心可编辑昵称、头像地址和手机号，并通过 Go `PATCH /me` 持久化。
- [x] 下拉菜单滚动锁定修复：shadcn/Radix `DropdownMenu` 默认关闭 modal 滚动锁定，避免打开下拉时右侧出现白色竖条。
- [x] 登录态刷新和账号缓存隔离修复：`/me` 优先走 Cookie，会话缺失时用本地登录账号 `X-User-ID` 兜底；切换账号会先清空本地历史/收藏再重拉当前账号数据。
- [x] 退出后历史/收藏回流修复：未登录状态下不再同步 `/me/watch-history` 和 `/me/favorites`，避免刷新后拉回默认 `demo-user` 数据。

## 推荐下一步顺序

1. 做播放页冒烟和组件测试
2. 继续真实用户体系：手动联调 session cookie、后续 JWT/服务端 session、头像上传和账号安全设置
3. 做后台/内容管理能力

## P0：近期优先做

### 1. 数据模型和 mock 数据整理

- [x] 把 `mock-videos.ts` 拆分为更清晰的数据模块，例如频道、视频、查询函数。
- [x] 统一视频字段命名，为后续后端接口对齐做准备。
- [x] 保持 `@/lib/mock-videos` 导出 API 不变，避免页面大面积改动。
- [x] 给常用查询函数补充覆盖：详情、频道、搜索、排行榜、相关推荐。
- [x] 验收标准：页面行为不变，相关 `node --test src\lib\*.test.mjs` 通过。

### 2. 全站加载态、空状态、错误态

- [x] 抽取通用 `EmptyState`、`LoadingState`、`ErrorState` 组件。
- [x] 搜索无结果、频道筛选无结果、历史为空、收藏为空、缓存为空使用统一空状态。
- [x] 为未来接口失败预留错误展示。
- [x] 验收标准：每个列表页都有明确的空态文案和返回/重试入口。

### 3. Header 和移动端体验继续完善

- [x] 移动端补频道入口或更多菜单，避免只能看到 logo、搜索和用户按钮。
- [x] 用户菜单增加“最近观看”快捷列表。
- [x] Header 当前频道高亮。
- [x] 验收标准：移动端 375px 宽度下不溢出，主要入口都可访问。

### 4. 播放器体验增强

- [x] 增加播放页“下一集”按钮。
- [x] 视频结束后自动推荐下一集或相关推荐。
- [x] 选集切换时保留更自然的返回路径。
- [x] 弹幕开关、透明度、速度设置。
- [x] 验收标准：多集视频可以从第 1 集连续看下去，弹幕设置可即时生效。

## P1：产品功能增强

### 5. 真实登录态模拟升级

- [x] 增加登录弹窗或登录页。
- [x] 用户 store 增加头像、手机号/邮箱、会员到期提醒。
- [x] 未登录时限制收藏、缓存、评论等操作，并提示登录。
- [x] 登录弹窗支持邮箱密码登录/注册二合一。
- [x] 登录/注册成功后，前端持久化后端用户 id，并自动带 `X-User-ID` 请求头。
- [x] API 请求支持 `credentials: "include"`，可配合 Go 后端 `next_video_session` Cookie 维持登录态。
- [x] 个人中心支持编辑昵称、头像地址和手机号，并同步 Go `PATCH /me`。
- [x] 登录失败、注册重复邮箱、弱密码等前端错误提示。
- [x] 验收标准：未登录、普通用户、VIP 用户三种状态有清晰差异。

### 6. VIP 权益闭环

- [x] VIP 页面点击套餐后更新本地 VIP 状态。
- [x] VIP 页面点击套餐后同步 Go `/me/vip`，写入后端账号 VIP 状态。
- [x] 会员内容在卡片上增加 VIP 标识。
- [x] 非 VIP 用户播放会员内容时显示权益提示。
- [x] 验收标准：VIP 状态会影响 Header、个人中心、VIP 页和播放页提示。

### 7. 搜索体验增强

- [x] 搜索输入支持清空按钮。
- [x] 搜索页增加“按频道/年份/清晰度”更多筛选。
- [x] 热搜榜增加排名和热度。
- [x] 搜索结果支持关键词高亮。
- [x] 验收标准：搜索页可以处理空输入、无结果、筛选组合和历史回填。

### 8. 排行榜增强

- [x] 排行榜增加频道筛选，例如电影榜、剧集榜、综艺榜。
- [x] 增加飙升榜、口碑榜、VIP 榜。
- [x] 首页增加排行榜入口模块。
- [x] 验收标准：用户能从首页、Header、频道导航进入排行榜。

### 9. 评论和弹幕增强

- [x] 评论支持点赞、删除自己的评论。
- [x] 评论区支持排序：最新、最热。
- [x] 弹幕支持颜色选择和发送频率限制。
- [x] 验收标准：同一视频同一集的互动数据互不串扰。

## P2：接后端和长期建设

### 10. API 数据层

- [x] 新建 API client 层，集中管理请求、错误和 mock fallback。
- [ ] 将 mock 查询函数逐步替换为 API 调用。
- [x] 保留 mock fallback，方便本地演示。
- [x] 排行榜页作为首个试点接入 API facade。
- [x] 首页、频道页、搜索页和播放详情页接入 API facade。
- [x] 用户资料、追剧收藏、观看历史接入 Go `/me` API facade。
- [x] 评论和弹幕接入 Go 视频互动 API facade。
- [x] 播放详情页接入 Go `playback` 配置，mock fallback 保持同形数据。
- [x] VIP 套餐购买接入 Go `/me/vip` API facade，mock fallback 保持本地开通体验。
- [ ] 验收标准：页面组件不直接拼接口地址，数据入口集中。

### 11. 后台内容管理

- [ ] 视频管理：新增、编辑、上下架。
- [ ] 频道管理：频道排序、频道配置。
- [ ] 推荐位管理：首页主推、热播、榜单。
- [ ] 验收标准：前台展示内容可以由后台配置驱动。

### 12. 真实播放器能力

- [x] 支持 HLS/DASH 播放源。
- [x] 支持播放配置由接口返回。
- [x] 支持清晰度切换控件读取接口播放源。
- [x] 支持试看秒数和会员内容标记来自接口。
- [x] 后端支持独立多播放源表，同一视频可返回多档清晰度。
- [x] 支持后端账号 VIP 鉴权驱动 `canPlay`。
- [x] 支持前端 VIP 套餐购买状态同步到后端账号。
- [x] 支持前端 HLS/DASH 播放引擎。
- [x] 支持播放器控制菜单读取接口播放源，并避免原生 select 在深色播放器里不可读。
- [x] 支持断点续播同步到服务端。
- [ ] 验收标准：播放进度、清晰度和权限由接口返回控制。

### 13. 测试和质量保障

- [ ] 增加组件级测试，覆盖 Header、搜索、播放页关键交互。
- [ ] 增加 Playwright 冒烟测试：打开首页、搜索、播放、个人中心。
- [x] 新增播放页 Playwright 冒烟入口：手动启动前端后可验证播放页加载和清晰度菜单切换。
- [ ] 增加移动端视口截图检查。
- [ ] 验收标准：核心路径在提交前可以自动验证。

## 建议下一次开始做

优先做 **session cookie 手动联调** 和 **播放页冒烟/组件测试**，后续再升级 JWT/服务端 session。

原因：

- 后端已经支持邮箱密码注册/登录，登录/注册成功会写入 `next_video_session` httpOnly Cookie；前端请求已带 `credentials: "include"`，页面启动同步 `/me` 时优先用 Cookie 判断真实登录态，同时其他账号接口仍兼容登录态自动带当前用户 `X-User-ID`。
- 首页、频道页、搜索页、播放详情页已经通过 API facade 接入 Go 视频只读接口。
- 用户资料、追剧收藏、观看历史已经接入 Go `/me` API，并完成本地联调验证。
- 评论和弹幕已经接入 Go 视频互动 API，并保留本地 fallback。
- 播放源、清晰度、试看秒数、会员内容标记和断点续播恢复点已经由播放详情接口返回，多播放源表、后端 VIP 鉴权和 VIP 购买状态写回都已经落库，前端也已按 `mimeType` 接入 HLS/DASH 播放引擎；下一步适合补播放页冒烟验证。

建议拆分：

1. [x] 后端为播放详情返回播放源、清晰度列表、试看限制和会员权限字段。
2. [x] 前端播放器读取后端播放配置，保留 mock fallback。
3. [x] 支持清晰度切换和试看字段来自接口。
4. [x] 新增后端多播放源表，支持同一视频多清晰度。
5. [x] 接入后端账号 VIP 鉴权，让 `canPlay` 和试看策略由用户状态计算。
6. [x] 新增前端到后端的 VIP 状态同步，让 VIP 页面套餐选择写入 `users.is_vip/vip_until`。
7. [x] 将断点续播策略与现有 `/me/watch-history` 进一步对齐。
8. [x] 按 `mimeType` 接入 HLS/DASH 前端播放引擎。
9. [x] 增加播放页 Playwright 冒烟入口，覆盖清晰度切换和播放页加载。
10. [x] 继续补首页、搜索、个人中心等核心路径 Playwright 冒烟测试文件；当前以手动页面自测为主，自动 e2e 需要先启动 Next 服务并配置正确 baseURL。
11. [x] 新增真实登录注册 v1：后端 `POST /me/register`、邮箱密码 `POST /me/login`、前端登录/注册弹窗和登录态 `X-User-ID` 请求头。
12. [x] 联调真实登录注册 v1，补登录失败、注册重复邮箱、弱密码等前端错误提示。
13. [x] 完成 session cookie v1：Go 登录/注册写入 httpOnly Cookie，前端 API 请求带 `credentials: "include"`，并继续兼容开发期 `X-User-ID`。
14. [x] 完成前端 Cookie 会话恢复 v1：`useUserStore.syncFromApi` 探测 `/me` 时不带 `X-User-ID`，避免过期本地账号状态掩盖 Cookie 登录态。
15. [x] 完成用户资料编辑 v1：Go `PATCH /me` 保存昵称、头像地址和手机号，个人中心概览页提供编辑入口。
16. [x] 完成登录态刷新兜底和账号缓存隔离修复：Cookie 会话缺失时回退到本地持久化账号，登录/注册/退出和账号切换时隔离本地观看历史与收藏。
17. [x] 完成退出后历史/收藏回流修复：未登录时历史/收藏 store 直接清空本地列表，不请求账号 API，避免默认 `demo-user` 数据回填。
18. [ ] 手动复测 cookie 登录态刷新保持、退出清 Cookie、退出后刷新历史/收藏为空、资料编辑保存、跨账号历史/收藏隔离，并继续补播放页冒烟/组件测试。
